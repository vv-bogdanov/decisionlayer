[project]
name = "repo-decisions"
version = "0.1.0"
description = "Codex ADR decision layer plugin POC"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
  "coverage[toml]>=7.6",
  "pytest>=8",
]

[tool.coverage.run]
branch = true
source = ["repo_decisions"]
omit = [
  "plugins/repo-decisions/repo_decisions/*",
]

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 80

[tool.pytest.ini_options]
testpaths = ["tests"]
